pub const POOL: &[u8] = b"pool";
pub const VAULT: &[u8] = b"vault";

pub const MAX_NAME_LEN: usize = 32;

/// Maximum swap amount in whole dollars (e.g., 1000 = $1000)
pub const MAX_SWAP_DOLLARS: u64 = 2000;
